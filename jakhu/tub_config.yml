# Sample tub config
# Name of tub (BOSS_WORKER_NAME)
# The rest is in config
name: default

# Lang
language: nodejs
version: stable

# Web-app dir (BOSS_WEB_DIR)
dir: vagrant

# Web-app name (BOSS_APP_NAME)
app: foo
